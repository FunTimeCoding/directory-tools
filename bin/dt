#!/usr/bin/env python3
from sys import argv, exit

from directory_tools.directory_tools import DirectoryTools

if __name__ == '__main__':
    application = DirectoryTools(argv[1:])
    exit_code = application.run()
    exit(exit_code)
