#!/usr/bin/env python3
import sys

from directory_tools.directory_tools import DirectoryTools

if __name__ == '__main__':
    application = DirectoryTools(sys.argv[1:])
    exit_code = application.run()
    sys.exit(exit_code)
